/*
 * Disclaimer Messages
 *
 * This contains all the text for the Disclaimer component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.GroupDiagram';

export default defineMessages({
  caption: {
    id: `${scope}.caption`,
    defaultMessage: '{title} - Ecological Traits & Key Ecological Drivers',
  },
});
