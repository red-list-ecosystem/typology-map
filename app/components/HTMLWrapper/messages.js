/*
 * Disclaimer Messages
 *
 * This contains all the text for the Disclaimer component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.HTMLWrapper';

export default defineMessages({
  showMore: {
    id: `${scope}.showMore`,
    defaultMessage: 'Read more',
  },
});
