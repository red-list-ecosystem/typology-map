/*
 * Breadcrumb Messages
 *
 * This contains all the text for the Disclaimer component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Breadcrumb';

export default defineMessages({
  explore: {
    id: `${scope}.explore`,
    defaultMessage: 'All',
  },
});
